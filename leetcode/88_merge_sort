def merge(nums1, m, nums2, n):
        """
        Do not return anything, modify nums1 in-place instead.
        """
        count = 0
        for i in range(m,len(nums1)):
            nums1[i] = nums2[count]
            count +=1
        print(nums1)

nums1 = [1,2,3,0,0,0]
m = 3
nums2 = [2,5,6]
n = 3
value = merge(nums1,m,nums2,n)